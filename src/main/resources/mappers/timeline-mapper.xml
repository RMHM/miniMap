<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="TimeLine">

	<select id="selectMno" parameterType="string" resultType="_int">
		SELECT MNO FROM MEMBER WHERE MNICK=#{mnick}
	</select>

	<insert id="insertTimeLine" parameterType="TimeLineSmpl">
		INSERT INTO TIMELINE VALUES (SEQ_TID.NEXTVAL, #{mNo}, DEFAULT, #{tContent}, DEFAULT)
	</insert>
	
	<select id="getMemberByNick" parameterType="string" resultType="Member">
		SELECT * FROM MEMBER WHERE MNICK=#{mnick}
	</select>
	
	<select id="selectPreTimeLine" resultType="list">
		SELECT * FROM (SELECT * FROM TIMELINE ORDER BY TID DESC) WHERE <![CDATA[ROWNUM<=20]]>
	</select>
	
</mapper>