<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	
   <resultMap type="Member" id="CommonMemberMap">
      <result column="MNO" property="mno" />
      <result column="MID" property="mid" />
      <result column="JOIN_DATE" property="joinDate" javaType="java.sql.Date" jdbcType="DATE" /> 
      <result column="DROP_DATE" property="dropDate" javaType="java.sql.Date" jdbcType="DATE" />
      <result column="EMAIL" property="email" />
      <result column="GENDER" property="gender" />
      <result column="AGE" property="age" /> 
   </resultMap>
   
   <resultMap type="Member" id="CompMemberMap">
      <result column="MNO" property="mno" />
      <result column="MID" property="mid" />
      <result column="JOIN_DATE" property="joinDate" javaType="java.sql.Date" jdbcType="DATE" /> 
      <result column="DROP_DATE" property="dropDate" javaType="java.sql.Date" jdbcType="DATE" />
      <result column="EMAIL" property="email" />
   </resultMap>

	<insert id="insertMember" parameterType="Member">
		insert into member values(
		seq_mno.nextval, #{mid}, #{mpw}, #{mname}, #{mnick}, 
		#{mtype}, #{email}, #{gender}, #{age}, #{profilePath}, DEFAULT, DEFAULT)
	</insert>

	<select id="selectOne" parameterType="string" resultType="Member">
		select * from member where mid = #{mid}
	</select>
	
	<select id="selectFindID" parameterType="string" resultType="Member">
		select * from member where email = #{email}
	</select>
	
	<select id="selectCommonMember" resultMap="CommonMemberMap">
		SELECT * FROM MEMBER WHERE MTYPE = 'M'
	</select>
	
	<select id="selectCompanyMember" resultMap="CompMemberMap">
		SELECT * FROM MEMBER M
		JOIN AUTHORITY A USING(MNO)
		JOIN AUTHORITY_TYPE AT ON (A.ACODE = AT.ACODE)
		WHERE MTYPE = 'C'
	</select>
	
	<!-- <select id="selectBlackList" resultMap="selectBlackMap">
	
	</select> -->

</mapper>
